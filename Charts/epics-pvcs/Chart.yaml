# this chart should be installed once per beamline and be the first
# chart installed as it creates resources share by all iocs and other
# EPICS services

apiVersion: v2
name: epics-pvcs
description: |
  A set of PVCs to share between pods for a services deployment

# version is replaced with git tag by the CI
version: 1.0.0

type: application
home: https://github.com/epics-containers/ec-helm-charts
sources:
  - https://github.com/epics-containers/ec-helm-charts/tree/main/Charts/epics-pvcs
maintainers:
  - name: epics-containers
    url: https://epics-containers.github.io
