# this chart should be installed once per beamline and be the first
# chart installed as it creates resources share by all iocs and other
# EPICS services

apiVersion: v2
name: epics-pvcs
description: |
  A set of PVCs to share between pods for a services domain

type: application

version: 3.4.0
